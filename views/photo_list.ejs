<%- include('search_bar', data); -%> 

<div class="photo_list content">
    <% Object.entries(data).forEach( ([cam_id, camera]) => { %>
        
        <% if (camera.photos.length) { %>

            <div data-id="<%= cam_id %>" class="camera">
            
                <h4><%= camera.name %></h4>

                <div class="photos">
                    <% camera.photos.forEach( photo => { %>
                        <div data-id="<%= photo.id %>" class="photo">
                            <img src="<%= photo.url %>">
                            <img class="icon_photo" src="/static/images/image.svg">
                            <h3><%= photo.title %></h3>
                        </div>
                    <% }); %>
                </div>
            
            </div>

        <% } %>
    <% }); %>
</div>

<script src="/static/js/photo_list.js"></script>